# v1.1.0 迭代记录

## Keep / Adapt / Reject / Invent

- Keep：来源定位、结构化 note JSON、安全本地 HTML、单文件打包。
- Adapt：本地提取器增加 PDF 文字层与逐页主图；网页从章节卡片扩展为三级标签、搜索、筛选与原页预览。
- Reject：把图片型 PDF 当作空白页；仅用颜色表达标签；默认展开全部细节；为装饰而配图。
- Invent：统一页码主键、`needs_visual_review`、核心20%/支撑80%优先级和80/20行动批注协议。

## 研究证据

先例研究使用 `starline-meta-skill/scripts/research_prior_art.py`。SkillsMP 返回15个候选；skills.sh 因环境缺少 `npx` 未运行。采用可访问性与渐进披露方向，拒绝与学习综合无关的PDF编辑和TUI候选。

## 验证

- PDF 单页定位与视觉复核标记单元测试。
- 现有 TXT、渲染安全和单文件打包回归测试。
- 真实82页图片型PDF试运行，生成逐页标签索引与桌面/移动端截图。
